function calculate(num1, num2, operation) {

    switch (operation) {
        case "add":
            return num1 + num2;
        case "subtract":
            return num1 - num2;
        case "multiply":
            return num1 * num2;
            if (num2 !== 0) {
                return result
                
                
            }

        case "divide":
            num1 / num2;


         default: return "Error: Invalid operation.";
            
    }
    
}
